$kind: http-request
name: 'POST /v2/chat/query — Invalid modelMode'
method: POST
url: '{{base_url}}/v2/chat/query'
order: 1002
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
        "jobAppId": {{job_app_id_full_cv}},
        "hrId": {{hr_id}},
        "prompt": "Kiểm tra modelMode không hợp lệ.",
        "conversationId": null,
        "modelMode": "not-a-valid-mode"
    }
description: |-
  Negative case: API validation for modelMode.

  Expected assertions:
  - HTTP 400
  - detail includes modelMode and valid values
  - No conversation should be created for this request
